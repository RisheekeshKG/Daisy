node_modules/
dist/
release/

backend/build/
# Regenerated by scripts/build-backend.mjs (--specpath) on every build, so it
# is output, not a checked-in input.
backend/daisy-backend.spec
coverage/
.DS_Store
*.log
.env*
!.env.example

# Python backend
.venv/
backend/.venv/
backend/__pycache__/
**/__pycache__/
*.pyc
.pytest_cache/

# Piper TTS voice models + faster-whisper STT model (downloaded via `npm run backend:setup`)
backend/voices/
backend/whisper-model/
